import React, { Component } from 'react';
import Box from '../containers/box-model';

export default class App extends Component {
  render() {
    return (
      <div className="box-model">
        <Box/>
      </div>

    );
  }
}
